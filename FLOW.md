# FLOW.md — Moved

This document has been moved to [`docs/FLOW.md`](docs/FLOW.md).

It contains the full bilingual (English + Chinese) execution flow reference: bootstrap sequence, pack discovery, server tick scan, RuleEngine evaluation, all event hook flows, Lucky drop pipeline, LegacyCommandRunner dispatch, `/reload` phase table, and trigger type reference.
